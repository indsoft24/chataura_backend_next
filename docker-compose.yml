services:
  nginx:
    image: nginx:1.25-alpine
    container_name: chataura_nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./chataura_backend_nest/docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - nestjs-api
    networks:
      - chataura_net
    profiles:
      - full

  nestjs-api:
    build:
      context: ./chataura_backend_nest
      dockerfile: docker/Dockerfile
      target: production
    container_name: chataura_api
    restart: unless-stopped
    ports:
      - "3005:3000"
    env_file:
      - ./chataura_backend_nest/.env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - API_PREFIX=${API_PREFIX:-api/v2}
      - PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-https://chataura.in}
      - DATABASE_URL=postgresql://chataura_user:${DB_PASSWORD:-chataura_dev}@postgres:5432/chataura_db?schema=public&connection_limit=25
      - REDIS_URL=redis://redis:6379
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - NODE_OPTIONS=--max-old-space-size=1200
    deploy:
      resources:
        limits:
          memory: 1500M
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - chataura_net
    profiles:
      - full

  admin-panel:
    build:
      context: ./chataura_admin
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api/v2}
        NEXT_PUBLIC_BASE_PATH: ${NEXT_PUBLIC_BASE_PATH:-/nextadmin}
    container_name: chataura_admin_panel
    restart: unless-stopped
    ports:
      - "3100:3100"
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-/api/v2}
      - NEXT_PUBLIC_BASE_PATH=${NEXT_PUBLIC_BASE_PATH:-/nextadmin}
    depends_on:
      - nestjs-api
    networks:
      - chataura_net
    profiles:
      - full

  postgres:
    image: postgres:16-alpine
    container_name: chataura_postgres
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: chataura_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-chataura_dev}
      POSTGRES_DB: chataura_db
    command: ["postgres", "-c", "config_file=/etc/postgresql/postgresql.conf"]
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./chataura_backend_nest/docker/postgres/postgresql.conf:/etc/postgresql/postgresql.conf:ro
    deploy:
      resources:
        limits:
          memory: 2500M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chataura_user -d chataura_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - chataura_net

  redis:
    image: redis:7-alpine
    container_name: chataura_redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    command: >
      redis-server
      --maxmemory 700mb
      --maxmemory-policy allkeys-lru
      --appendonly yes
      --save 900 1
      --save 300 10
    volumes:
      - redis_data:/data
    deploy:
      resources:
        limits:
          memory: 800M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - chataura_net

volumes:
  postgres_data:
  redis_data:
  uploads_data:

networks:
  chataura_net:
    driver: bridge

# ChatAura Admin

Sibling Next.js 14 panel for Nest `/api/v1/admin/*`. Deploy to Vercel; it does not run on the VPS.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Sign in with a Nest user whose `role` is `admin`.
